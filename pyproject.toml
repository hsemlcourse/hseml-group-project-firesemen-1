[project]
name = "ethusd-cp3-deploy"
version = "0.3.0"
description = "CP3 deployment for ETHUSD one-minute price prediction"
requires-python = ">=3.10"

[tool.ruff]
line-length = 110
src = ["src", "app", "tests"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
ignore = ["B008"]
